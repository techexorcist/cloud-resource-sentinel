# Mock data profile — no AWS credentials required.
# Activate with: --spring.profiles.active=mock-data
# or: SPRING_PROFILES_ACTIVE=mock-data ./mvnw spring-boot:run

# Override AI to disabled by default in mock mode (can still enable manually)
cloud-sentinel.scheduled-scan.enabled=false
